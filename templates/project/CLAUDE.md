# {{PROJECT_NAME}} — Project Constitution

Loaded by every session inside `projects\{{PROJECT_NAME}}\`, after the root CLAUDE.md (root rules apply; this file adds project specifics).

## What this project is

{{PROJECT_DESCRIPTION}}

## Components & sessions

| Folder | Agent for | Discord channel |
|---|---|---|
| {{COMPONENT}}\ | {{COMPONENT_PURPOSE}} | #{{COMPONENT}} |

## Project rules

- Your world is this project; your desk is your component folder (root CLAUDE.md §4).
- Memory: index in `memory\MEMORY.md`; your live notes in `memory\sessions\<component>.md` (copy `_template.md`). Record interfaces and decisions the moment they stabilize — your sibling sessions build on them.
- This project may become its own git repo, but only when he asks for one — a
  new project is not given one by default (it might be throwaway). Once it has
  a repo, commit early and often.

## Tech stack & key decisions

*(filled in as the project evolves — keep this the quick-orientation section for any new session)*
